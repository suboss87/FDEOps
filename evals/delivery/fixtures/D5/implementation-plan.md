# Implementation plan (engineering pack)

Repository conventions: Node 18, `npm test`, no new dependencies without a decision line.

Test commands:
```
npm test
```

Carry customer acceptance from `.fde/success.md` and evidence into `delivery.md` after the change.
